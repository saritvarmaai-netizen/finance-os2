// src/lib/format.ts
// Re-export from utils.ts for backward compatibility
// All formatting functions are now centralized in utils.ts

export { fmt, formatINR, formatPercent, formatDate, getEntityColor } from './utils'
